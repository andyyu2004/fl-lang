sig test :: Int
let test = 5 + 3 * 9 + 8 - 88 / 888

let fuck = x

// f :: Int -> Bool -> Int
// f = -(5 + 3)
// tuple :: (Int, Bool)
// tuple = (5, 6)

sig compose :: Int
let weirdcompose f g x  = f g x
